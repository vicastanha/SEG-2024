export interface ICredential {
    username: string;
    password: string;
  }
  export interface IUser {
    uid?: string;
    name?: string;
    name: string;
    email: string;
    password?: string;
  }